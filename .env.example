# Stripe test secret key used for `stellar-card auth login` (Issuing test mode).
STELLAR_CARD_API_KEY=sk_test_...

# Optional: relocate the CLI state directory (default: ~/.config/stellar-card).
# STELLAR_CARD_HOME=/path/to/state

# Web demo (used by web/.env.local, not by the CLI).
NEXT_PUBLIC_FEE_VAULT_CONTRACT_ID=C...
